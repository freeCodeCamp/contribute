// Used by starlight's SiteTitle component, but not declared.
declare module 'virtual:starlight/user-images' {
  const logos: {
    light: {
      src: string;
      width: number;
      height: number;
    };
    dark: {
      src: string;
      width: number;
      height: number;
    };
  };
  export { logos };
}
